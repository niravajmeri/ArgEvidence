<h1>Listing premise_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @premise_types.each do |premise_type| %>
  <tr>
    <td><%= premise_type.name %></td>
    <td><%= link_to 'Show', premise_type %></td>
    <td><%= link_to 'Edit', edit_premise_type_path(premise_type) %></td>
    <td><%= link_to 'Destroy', premise_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Premise type', new_premise_type_path %>
