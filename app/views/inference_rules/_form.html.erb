<%= form_for(@inference_rule) do |f| %>
  <% if @inference_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inference_rule.errors.count, "error") %> prohibited this inference_rule from being saved:</h2>

      <ul>
      <% @inference_rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
