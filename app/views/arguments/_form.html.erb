<%= form_for(@argument) do |f| %>
  <% if @argument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@argument.errors.count, "error") %> prohibited this argument from being saved:</h2>

      <ul>
      <% @argument.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= f.label :title %><br />
    <table>
      <tr>
        <td> Title
        </td>
        <td>
          <%= f.text_field :title %>
        </td>
      </tr>
      <tr>
        <td> Description
        </td>
        <td>
          <%= f.text_area :description, :rows => "3"%>
        </td>
      </tr>
	  <tr>
		<td> Evidence
		</td>
		<td>
			 <%#= select_tag("argument[evidence_ids][]", options_for_select(Evidence.all.collect { |evidence| [evidence.title, evidence.id] }, @argument.evidences.collect { |evidence| evidence.id}), {:multiple=>true, :size=>5}) %>
			 
			 
			 	<% Evidence.all.each do |evidence| %>
					<label class="li"><%= check_box_tag :evidence_ids, evidence.id, @argument.evidences.include?(evidence), :name => 'argument[evidence_ids][]' %>
					<%= evidence.title %></label><br />
				<% end %>
			 
 
		</td>
	  </tr>
      <tr>
        <td colspan="2">
           <%= f.submit %> or <%= link_to 'Cancel', arguments_path %>
        </td>

      </tr>
    </table>
  <!-- </div>
  <div class="field">
  -->
    <%#= f.label :description %>

  </div>
  <!--
  <div class="field">
    <%#= f.label :user_id %><br />
    <%#= f.number_field :user_id, :value=> :current_user %>
    <%#= select_tag "user_id", options_from_collection_for_select(@users, "id", "email"), :include_blank => true %>

  </div>
  -->
  <!--<div class="actions">
    
  </div>-->
<% end %>
