<%= form_for(@premise) do |f| %>
  <% if @premise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@premise.errors.count, "error") %> prohibited this premise from being saved:</h2>

      <ul>
      <% @premise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scheme_id %><br />
    <%= f.number_field :scheme_id %>
  </div>
  <div class="field">
    <%= f.label :premise_type_id %><br />
    <%= f.number_field :premise_type_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
